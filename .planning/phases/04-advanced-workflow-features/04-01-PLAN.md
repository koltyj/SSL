---
phase: 04-advanced-workflow-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ssl-matrix-client/templates.py
  - tests/test_templates.py
autonomous: true
requirements: [SESS-01, SESS-02]
must_haves:
  truths:
    - "Console state can be serialized to a JSON template file at ~/.ssl-matrix/templates/"
    - "A template file can be deserialized back into typed Python dicts matching ConsoleState structure"
    - "Diff between template state and current ConsoleState shows changes grouped by category"
    - "Template apply sends correct builder calls in correct order for routing restore"
    - "XPatch is stored in template but skipped on restore with a warning"
    - "Template names are auto-generated from console project title + timestamp"
  artifacts:
    - path: "ssl-matrix-client/templates.py"
      provides: "Template save/load/diff/apply core logic"
      exports: ["save_template", "load_template", "list_templates", "delete_template", "show_template", "diff_template", "apply_template", "capture_template_state", "make_template_name", "TEMPLATE_DIR"]
    - path: "tests/test_templates.py"
      provides: "Unit tests for template serialization, CRUD, diff, routing restore"
      min_lines: 100
  key_links:
    - from: "ssl-matrix-client/templates.py"
      to: "ssl-matrix-client/models.py"
      via: "dataclasses.asdict for serialization"
      pattern: "asdict.*ConsoleState|Channel|DawLayer|InsertDevice"
    - from: "ssl-matrix-client/templates.py"
      to: "~/.ssl-matrix/templates/"
      via: "pathlib.Path file I/O"
      pattern: "TEMPLATE_DIR.*json"
---

<objective>
Build the template core module that serializes/deserializes ConsoleState to JSON files at ~/.ssl-matrix/templates/, with diff computation and routing restore apply logic.

Purpose: This is the foundation for SESS-01 (session templates) and SESS-02 (routing recall). The CLI commands (Plan 03) will call these functions directly.
Output: `ssl-matrix-client/templates.py` with full save/load/diff/apply API, plus comprehensive unit tests.
</objective>

<execution_context>
@/Users/koltonjacobs/.claude-gsd/get-shit-done/workflows/execute-plan.md
@/Users/koltonjacobs/.claude-gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-workflow-features/04-CONTEXT.md
@.planning/phases/04-advanced-workflow-features/04-RESEARCH.md

@ssl-matrix-client/models.py
@ssl-matrix-client/client.py
@tests/conftest.py

<interfaces>
<!-- Key types and contracts from models.py that templates.py must serialize -->

From ssl-matrix-client/models.py:
```python
@dataclass
class ConsoleState:
    desk: DeskInfo
    channels: list          # List[Channel] — 32 channels
    profiles: list          # List[ProfileItem]
    daw_layers: list        # List[DawLayer] — 4 layers
    devices: list           # List[InsertDevice] — 16 devices
    chains: list            # List[Chain]
    channel_inserts: list   # List[ChannelInserts]
    matrix_presets: list    # List[MatrixPreset]
    tr_enabled: bool
    xpatch: XpatchState
    softkeys: SoftkeysState
    automation_mode: int    # 0=Legacy, 1=Delta
    transport_lock_layer: int
    project_name: str
    title_name: str
    display_17_32: int
    flip_scrib: int
```

From ssl-matrix-client/client.py:
```python
class SSLMatrixClient:
    state: ConsoleState
    _lock: threading.Lock
    my_serial: int
    def send(self, data): ...
    def request_sync(self): ...
```

From ssl-matrix-client/handlers/routing.py (builders needed for restore):
```python
def build_set_insert_names_v2(desk_serial, my_serial, device, name, stereo): ...
def build_set_insert_to_chan_v2(desk_serial, my_serial, chan, device, slot): ...
def build_assign_chain_to_chan(desk_serial, my_serial, chan, chain_name): ...
```

From ssl-matrix-client/handlers/channels.py:
```python
def build_set_chan_name(desk_serial, my_serial, chan, name): ...
```

From ssl-matrix-client/handlers/profiles.py:
```python
def build_set_profile_for_daw_layer(desk_serial, my_serial, layer, profile_name): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create templates.py — template serialization, CRUD, and naming</name>
  <files>ssl-matrix-client/templates.py, tests/test_templates.py</files>
  <behavior>
    - TestTemplateSave: `capture_template_state(state)` returns dict with keys: channels, daw_layers, devices, channel_inserts, automation_mode, tr_enabled, display_17_32, flip_scrib, xpatch; xpatch key is present with nested structure
    - TestTemplateSave: `save_template(state, daw_project_path=None)` writes JSON file to tmp_path with version=1, saved_at timestamp, console_project_title from state, daw_project_path, and state dict
    - TestTemplateLoad: `load_template(path)` returns dict matching saved structure; round-trips correctly (save then load produces equivalent dict)
    - TestTemplateCRUD: `list_templates(template_dir)` returns list of (filename, console_project_title, saved_at) tuples sorted by date descending
    - TestTemplateCRUD: `delete_template(path)` removes file; raises FileNotFoundError if missing
    - TestTemplateCRUD: `show_template(path)` returns parsed template dict (same as load_template but semantically for "inspect")
    - TestTemplateNaming: `make_template_name("Session A")` produces `Session_A_YYYYMMDD_HHMMSS.json`; `make_template_name("(none)")` produces `unnamed_YYYYMMDD_HHMMSS.json`; `make_template_name("")` produces `unnamed_YYYYMMDD_HHMMSS.json`; non-alphanumeric chars replaced with underscore; truncated to 20 chars before timestamp
  </behavior>
  <action>
    Create `ssl-matrix-client/templates.py` with:

    1. `TEMPLATE_DIR = Path.home() / ".ssl-matrix" / "templates"` constant
    2. `ensure_template_dir()` — creates dir with `mkdir(parents=True, exist_ok=True)`
    3. `make_template_name(project_title: str) -> str` — sanitize title (strip non-alphanum to `_`, truncate 20 chars, default "unnamed" for empty/"(none)"), append `_YYYYMMDD_HHMMSS.json`
    4. `capture_template_state(state: ConsoleState) -> dict` — use `dataclasses.asdict()` on the restorable fields: channels, daw_layers, devices, channel_inserts, automation_mode, tr_enabled, display_17_32, flip_scrib, xpatch. Do NOT include desk, profiles list, directory, disk_info, tr_snapshots, softkeys, synced, motors_off, mdac_meters — those are runtime or non-restorable.
    5. `save_template(state: ConsoleState, template_dir: Path = None, daw_project_path: str = None) -> Path` — captures state, wraps in envelope `{version: 1, saved_at, console_project_title: state.title_name or state.project_name, daw_project_path, state: captured}`, writes JSON with `indent=2` to template_dir (default TEMPLATE_DIR). Returns path of created file.
    6. `load_template(path: Path) -> dict` — reads JSON, returns parsed dict. No deserialization back to dataclasses (callers use dict directly).
    7. `list_templates(template_dir: Path = None) -> list` — glob `*.json` in dir, parse each for console_project_title and saved_at, return sorted by saved_at descending as list of `(filename, title, saved_at)`.
    8. `delete_template(path: Path) -> None` — `path.unlink()`, raises FileNotFoundError if missing.
    9. `show_template(path: Path) -> dict` — alias for `load_template`.

    Use `tmp_path` fixture in tests (not real ~/.ssl-matrix). Pass `template_dir` param to functions to enable test isolation.

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /Users/koltonjacobs/DEV/SSL-BOARD && python3 -m pytest tests/test_templates.py -x -q</automated>
  </verify>
  <done>All template save/load/CRUD/naming tests pass. templates.py exists with all exported functions. JSON files are human-readable with indent=2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add diff and apply logic to templates.py</name>
  <files>ssl-matrix-client/templates.py, tests/test_templates.py</files>
  <behavior>
    - TestTemplateDiff: `diff_template(template_data, current_state)` returns dict with keys: channels, profiles, routing, display, skipped. Each key holds list of human-readable change strings. Empty lists for unchanged categories.
    - TestTemplateDiff: Channel name change "Ch3: 'KICK' -> 'SNARE'" detected correctly
    - TestTemplateDiff: DAW layer protocol change detected
    - TestTemplateDiff: Insert device name change detected
    - TestTemplateDiff: Display setting (display_17_32, flip_scrib) changes detected
    - TestTemplateDiff: XPatch always appears in skipped with explanatory message
    - TestXpatchSkip: XPatch data is present in template JSON but `apply_template` never sends XPatch SET commands; skipped list includes XPatch warning
    - TestRoutingRestore: `build_apply_commands(template_data, current_state, categories, desk_serial, my_serial)` returns list of (builder_bytes, description) tuples in correct order: insert names first, then channel assignments. Only includes commands for categories in the `categories` set.
    - TestRoutingRestore: When categories={"routing"}, only routing commands returned. When categories={"channels"}, only channel name commands returned. When categories={"all"}, everything returned.
  </behavior>
  <action>
    Extend `ssl-matrix-client/templates.py` with:

    1. `diff_template(template_data: dict, current_state: ConsoleState) -> dict` — compare template["state"] against current ConsoleState. Group changes into categories:
       - "channels": compare channel names (use `state.channels[i].name` vs template channel name)
       - "profiles": compare daw_layers protocol and profile_name
       - "routing": compare devices (name, stereo, assigned), channel_inserts
       - "display": compare automation_mode, tr_enabled, display_17_32, flip_scrib
       - "skipped": always include "XPatch: SET commands fail silently on this console -- stored for reference only"
       Return dict of category -> list of change description strings. Empty list means no changes in that category.

    2. `build_apply_commands(template_data: dict, current_state: ConsoleState, categories: set, desk_serial: int, my_serial: int) -> list` — build list of `(bytes, str)` tuples where bytes is the UDP packet and str is a human-readable description. Only build commands for requested categories:
       - "channels": `build_set_chan_name` for each changed channel
       - "profiles": `build_set_profile_for_daw_layer` for each changed layer
       - "routing": First `build_set_insert_names_v2` for device names, then `build_set_insert_to_chan_v2` for channel assignments (ORDER MATTERS per Pitfall 3 in research)
       - "display": `build_set_auto_mode`, `build_set_display_17_32`, `build_set_flip_scrib_strip`, `build_set_tr_enable` as needed
       - "all": shorthand for all categories except skipped
       NEVER include XPatch SET commands.

    Import the builder functions from handlers modules. Thread safety note: callers must hold client._lock when reading current_state for diff, then release before sending commands.

    Write tests FIRST. Use mock/stub builder functions in tests — don't need real protocol encoding, just verify correct builders are called with correct args. Use `unittest.mock.patch` to intercept builder calls and verify arguments.
  </action>
  <verify>
    <automated>cd /Users/koltonjacobs/DEV/SSL-BOARD && python3 -m pytest tests/test_templates.py -x -q</automated>
  </verify>
  <done>Diff correctly identifies all category changes. Apply builds correct command sequence with routing order preserved. XPatch never included in apply commands. All template tests pass.</done>
</task>

</tasks>

<verification>
python3 -m pytest tests/test_templates.py -v
ruff check ssl-matrix-client/templates.py tests/test_templates.py
</verification>

<success_criteria>
- templates.py exists with save/load/diff/apply/CRUD functions
- All unit tests pass covering serialization round-trip, naming edge cases, diff detection, routing restore order, XPatch skip
- JSON template files are human-readable (indent=2)
- No external dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-workflow-features/04-01-SUMMARY.md`
</output>
