#!/bin/bash
# Launch ssl-matrix-client from anywhere.
# Usage: ssl          -> REPL
#        ssl tui      -> Terminal UI
#        ssl channels -> one-shot command
cd /Users/koltonjacobs/DEV/SSL-BOARD || exit 1
exec /usr/bin/python3 -m ssl-matrix-client "$@"
