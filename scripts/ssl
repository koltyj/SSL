#!/bin/bash
# Launch ssl-matrix-client from anywhere.
# Usage: ssl          -> REPL
#        ssl tui      -> Terminal UI
#        ssl channels -> one-shot command
cd "$(dirname "$0")/.." || exit 1
exec python3 -m ssl-matrix-client "$@"
